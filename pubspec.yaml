name: salon_booking_frontend
description: "A new Flutter project."
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.9.2
  provider: ^6.1.2
  shared_preferences: ^2.5.5
  google_fonts: ^6.2.0
  flutter_dotenv: ^5.2.1
  image_picker: ^1.2.0
  cupertino_icons: ^1.0.8
  intl: ^0.20.2
  flutter_map: ^8.3.1
  latlong2: ^0.9.1
  url_launcher: ^6.3.1
  geolocator: ^14.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
